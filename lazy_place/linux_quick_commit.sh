# Quickly add all new update to staging and commit then push to github
# This file is for windows

# add all file to staging
git add .

# commit the change
git commit -m "(auto) quickly commit"

# push update to github
git push
